import { PageWrapper } from "~/components";
import { GroupsList } from "~/groups";

const Page = () => {
  return (
    <PageWrapper title="Groups">
      <GroupsList />
    </PageWrapper>
  );
};

export default Page;
